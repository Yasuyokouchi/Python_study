class enzan:
    
    def __init__(self,x,y,z):
        self.x = x
        self.y = y 
        self.z = z

    def sum(self):
        res = 0 
        for num in range(salf.x,salf.y+1):
            res += num
            return res
        
class Main:

    def execute(self):
        calc_exec.sum()
        print("{}から{}の合計値は{}です。".format(calc_exec.x,calc_exec.y,exce.z,total)
